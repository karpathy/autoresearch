[project]
name = "autoresearch-forecasting"
version = "0.1.0"
description = "Autonomous time series forecasting & anomaly detection research"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24.0",
    "pandas>=2.0.0",
    "scikit-learn>=1.3.0",
    "torch>=2.0.0",
]
