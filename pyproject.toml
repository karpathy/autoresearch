[project]
name = "autoresearch-medical"
version = "0.1.0"
description = "Automated DL research for medical image classification on MedMNIST+"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "torchvision",
    "medmnist",
    "scikit-learn",
    "matplotlib",
    "numpy",
    "tqdm",
]
