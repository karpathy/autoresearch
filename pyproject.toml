[project]
name = "autotrader"
version = "0.1.0"
description = "Autonomous trading research — predict BTC forward returns"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.10.0",
    "numpy>=2.0.0",
    "pandas>=2.2.0",
    "pyarrow>=15.0.0",
    "requests>=2.32.0",
    "scikit-learn>=1.5.0",
    "torch>=2.5.0",
]
