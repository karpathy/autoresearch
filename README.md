# autosaas

AutoSaaS is the next iteration of this repository.

## Runtime layout

- `autosaas/` — active product package.
- `legacy/` — archived training-era files kept for reference.
- `program.md` — current operator playbook for AutoSaaS execution.

## Quick start

```bash
uv sync
uv run pytest tests/test_config.py -v
```
