# legacy

This directory stores archived training-era scripts and assets from the original repository.

The active AutoSaaS runtime and dependency surface now live in the top-level `autosaas/` package and root `pyproject.toml`.
