# skills
Portable instruction sets agents invoke. One markdown file per skill.

## Definitions
- `autonomous-iteration.md` | experiment loop protocol | edit-measure-keep/discard

## Constraints
- Skills reference workflow.yaml for config, never embed workflow-specific values -- portability
- Quality gates (self-eval, judge) are optional extensions in workflow.yaml -- not in skill defs
