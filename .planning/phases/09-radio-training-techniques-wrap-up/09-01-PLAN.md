---
phase: 09-radio-training-techniques-wrap-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - train.py
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-02
  - TRAIN-05

must_haves:
  truths:
    - "PHI-S transform applies Hadamard rotation + standardization to raw teacher features before loss"
    - "Feature Normalizer accumulates running statistics during warmup then whitens teacher features"
    - "Adaptor MLP v2 provides LayerNorm+GELU+residual alternative to simple linear projection"
    - "All three modules are gated by independent ENABLE_* flags defaulting to False"
  artifacts:
    - path: "train.py"
      provides: "PHISTransform, FeatureNormalizer, AdaptorMLPv2 classes + ENABLE flags"
      contains: "class PHISTransform"
    - path: "train.py"
      provides: "Hadamard matrix construction"
      contains: "_build_hadamard"
  key_links:
    - from: "PHISTransform"
      to: "teacher embeddings"
      via: "forward() applied before loss computation"
      pattern: "phi_s_transform"
    - from: "FeatureNormalizer"
      to: "teacher embeddings"
      via: "forward() with warmup-then-transform behavior"
      pattern: "feature_normalizer"
    - from: "AdaptorMLPv2"
      to: "projection heads"
      via: "replaces ProjectionHead when enabled"
      pattern: "AdaptorMLPv2"
---

<objective>
Implement three RADIO-inspired feature processing modules in train.py: PHI-S (Hadamard isotropic standardization), Feature Normalizer (per-teacher whitening), and Adaptor MLP v2 (LayerNorm+GELU+residual projection).

Purpose: These modules transform teacher/student features before loss computation, preventing gradient domination in multi-teacher distillation and improving feature alignment.
Output: Three nn.Module classes + ENABLE_* flags + Hadamard utility in train.py. Not yet wired into training loop (Plan 03 handles integration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-radio-training-techniques-wrap-up/09-CONTEXT.md
@.planning/phases/09-radio-training-techniques-wrap-up/09-RESEARCH.md
@train.py

<interfaces>
<!-- Existing train.py structure the executor needs to know -->

From train.py (module-level constants section, line ~46-74):
```python
# ============================================================
# EXPERIMENT VARIABLES (agent edits these)
# ============================================================
MODEL_NAME = "hf-hub:timm/lcnet_050.ra2_in1k"
BATCH_SIZE = 256
# ... other constants ...
TEACHER_CACHE_DIR = DEFAULT_TEACHER_CACHE_DIR
```

From train.py (model section, line ~80-93):
```python
class ProjectionHead(nn.Module):
    def __init__(self, in_features: int, out_features: int) -> None:
        super().__init__()
        hidden_dim = 512
        self.net = nn.Sequential(
            nn.Linear(in_features, hidden_dim),
            nn.BatchNorm1d(hidden_dim),
            nn.ReLU(inplace=True),
            nn.Linear(hidden_dim, out_features),
        )

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        return self.net(x)
```

From prepare.py (imported constants):
```python
EMBEDDING_DIM = 256  # Student embedding dimension
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PHI-S transform and Hadamard utility</name>
  <files>train.py</files>
  <read_first>
    - train.py (full file -- understand current structure, constants section, model section)
    - .planning/phases/09-radio-training-techniques-wrap-up/09-RESEARCH.md (PHI-S code example, Pattern 3 Hadamard construction, Pitfall 1 and 2)
    - RADIO/phi2.5.pdf (if needed for PHI-S formula verification)
  </read_first>
  <behavior>
    - _build_hadamard(8) returns (8,8) tensor with orthonormal rows (H @ H.T approx identity)
    - _build_hadamard(256) returns (256,256) tensor (power of 2, no padding needed)
    - _build_hadamard(3) raises AssertionError (not power of 2)
    - PHISTransform(256).ready == False before fit()
    - PHISTransform(256).fit(random_features_1000x256) sets ready=True
    - After fit, forward(batch_10x256) returns tensor shape (10, 256)
    - After fit, output has approximately zero mean and unit variance per dimension (within tolerance on random data)
    - PHISTransform(256).forward(x) when not ready returns x unchanged (passthrough)
  </behavior>
  <action>
    Add to train.py after the imports but before the EXPERIMENT VARIABLES section:

    1. Add `ENABLE_PHI_S = False` to the experiment variables section (per D-02). Add comment: "# PHI-S: Hadamard isotropic standardization for multi-teacher gradient balancing"

    2. Implement `_build_hadamard(n: int) -> torch.Tensor` utility function:
       - Sylvester recursive construction per PHI-S paper Eq. 18 (see 09-RESEARCH.md Pattern 3)
       - Assert n is power of 2
       - Return normalized Hadamard matrix (divide by sqrt(2) at each recursion step)

    3. Implement `class PHISTransform(nn.Module)` per D-06:
       - `__init__(self, feature_dim: int)`: register buffers for rotation matrix R (eye), scale alpha (1.0), mean (zeros). Set self.ready = False.
       - `fit(self, features: torch.Tensor)`: Compute mean, covariance, eigendecomposition via torch.linalg.eigh. Build Hadamard matrix (pad to next power of 2 if needed, truncate back). Compute R = H @ U^T, alpha = 1/phi where phi = sqrt(sum(eigenvalues)/D). Clamp eigenvalues min=1e-8 for numerical stability (per Pitfall 3 in research).
       - `forward(self, features: torch.Tensor) -> torch.Tensor`: If not ready, return features unchanged (passthrough during warmup). Otherwise: return alpha * (features - mean) @ R^T.

    IMPORTANT per D-06: PHI-S uses a FIXED Hadamard matrix -- no learnable params. The transform is computed once from training data statistics via fit(), then frozen.

    IMPORTANT per Pitfall 1: PHI-S operates on RAW features before L2 normalization. Add a code comment documenting this. For current single-teacher mode where cached embeddings are already L2-normalized, PHI-S has limited effect but is correctly implemented for multi-teacher activation.

    Place the class definition in the MODEL section of train.py (after ProjectionHead, before ArcMarginProduct).
  </action>
  <verify>
    <automated>cd /home/whiskey/workspace/project/central/v2/training/autoresearch && python -c "
import torch
from train import _build_hadamard, PHISTransform, ENABLE_PHI_S

# Verify flag exists and defaults False
assert ENABLE_PHI_S == False, 'ENABLE_PHI_S should default to False'

# Hadamard construction
H = _build_hadamard(256)
assert H.shape == (256, 256), f'Expected (256,256), got {H.shape}'
# Verify orthogonality: H @ H^T should be close to identity
eye_approx = H @ H.T
assert torch.allclose(eye_approx, torch.eye(256), atol=1e-5), 'Hadamard not orthogonal'

# PHI-S passthrough before fit
phi_s = PHISTransform(256)
assert phi_s.ready == False
x = torch.randn(10, 256)
out = phi_s(x)
assert torch.equal(out, x), 'Should passthrough before fit'

# PHI-S after fit
data = torch.randn(1000, 256)
phi_s.fit(data)
assert phi_s.ready == True
result = phi_s(data[:10])
assert result.shape == (10, 256), f'Expected (10,256), got {result.shape}'

print('PHI-S: ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ENABLE_PHI_S = False" train.py
    - grep -q "class PHISTransform" train.py
    - grep -q "_build_hadamard" train.py
    - grep -q "torch.linalg.eigh" train.py
    - grep -q "ready" train.py
  </acceptance_criteria>
  <done>PHISTransform class and _build_hadamard utility exist in train.py. ENABLE_PHI_S flag defaults to False. Transform correctly passes through before fit, and applies Hadamard rotation + standardization after fit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feature Normalizer with warmup</name>
  <files>train.py</files>
  <read_first>
    - train.py (current state after Task 1)
    - .planning/phases/09-radio-training-techniques-wrap-up/09-RESEARCH.md (Pattern 2 Feature Normalizer, Pitfall 3 numerical instability)
  </read_first>
  <behavior>
    - FeatureNormalizer(256, warmup_batches=5).ready == False initially
    - After feeding 4 batches via forward(), ready still False (passthrough)
    - After feeding 5th batch, ready becomes True
    - After ready, forward(batch) returns standardized features (approx zero mean, unit var per dim)
    - Forward returns same shape as input
    - Handles edge case: single-sample batch during warmup does not crash
  </behavior>
  <action>
    Add to train.py:

    1. Add `ENABLE_FEATURE_NORMALIZER = False` to experiment variables section (per D-02). Add comment: "# Feature Normalizer: per-teacher whitening + rotation"
    2. Add `NORMALIZER_WARMUP_BATCHES = 200` as a tunable constant (Claude's discretion -- 1 full epoch worth of batches for ~50k images / 256 batch size).

    3. Implement `class FeatureNormalizer(nn.Module)` per D-04 and D-07:
       - `__init__(self, feature_dim: int, warmup_batches: int = 200)`: Register buffers for running_mean (zeros), running_var (ones), count (0). Set self.ready = False.
       - `forward(self, features: torch.Tensor) -> torch.Tensor`:
         - If not ready: call _update_stats(features.detach()), return features unchanged (passthrough)
         - If ready: return (features - running_mean) / (running_var.sqrt() + 1e-6)
       - `_update_stats(self, features)`: Welford's online algorithm for running mean/variance. Increment count. When count >= warmup_batches, set ready = True.

    IMPORTANT per D-07: Statistics computed from training set during first epoch. The normalizer is per-teacher (each teacher gets its own FeatureNormalizer instance in the training loop -- handled in Plan 03 integration).

    IMPORTANT per Pitfall 3: Add epsilon (1e-6) to variance before inverse sqrt to prevent NaN. Add code comment about numerical stability.

    Place after PHISTransform class.
  </action>
  <verify>
    <automated>cd /home/whiskey/workspace/project/central/v2/training/autoresearch && python -c "
import torch
from train import FeatureNormalizer, ENABLE_FEATURE_NORMALIZER, NORMALIZER_WARMUP_BATCHES

# Flag check
assert ENABLE_FEATURE_NORMALIZER == False
assert NORMALIZER_WARMUP_BATCHES == 200

# Warmup behavior
fn = FeatureNormalizer(256, warmup_batches=5)
assert fn.ready == False

# Feed 4 batches -- should still passthrough
for i in range(4):
    x = torch.randn(32, 256)
    out = fn(x)
    assert torch.equal(out, x), f'Batch {i}: should passthrough'
assert fn.ready == False

# 5th batch triggers ready
x = torch.randn(32, 256)
out = fn(x)
assert fn.ready == True

# Now it should normalize
x2 = torch.randn(100, 256)
out2 = fn(x2)
assert out2.shape == (100, 256)
# Should not be identical to input anymore
assert not torch.equal(out2, x2), 'Should transform after ready'

print('FeatureNormalizer: ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ENABLE_FEATURE_NORMALIZER = False" train.py
    - grep -q "NORMALIZER_WARMUP_BATCHES = 200" train.py
    - grep -q "class FeatureNormalizer" train.py
    - grep -q "1e-6" train.py
  </acceptance_criteria>
  <done>FeatureNormalizer class exists in train.py with warmup accumulation and standardization. ENABLE_FEATURE_NORMALIZER and NORMALIZER_WARMUP_BATCHES flags exist. Passthrough during warmup, then normalizes after sufficient batches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adaptor MLP v2 projection head</name>
  <files>train.py</files>
  <read_first>
    - train.py (current state after Task 2)
    - .planning/phases/09-radio-training-techniques-wrap-up/09-RESEARCH.md (Adaptor MLP v2 code example from RADIOv2.5 Section 2.2)
  </read_first>
  <behavior>
    - AdaptorMLPv2(768, 256) creates a 2-layer MLP with LayerNorm+GELU (no residual since in!=out)
    - AdaptorMLPv2(256, 256) creates a 2-layer MLP with residual connection (in==out)
    - Forward pass: input (B, 768) -> output (B, 256)
    - Forward pass with residual: input (B, 256) -> output (B, 256), output != input (not identity)
    - Gradients flow through both layers
  </behavior>
  <action>
    Add to train.py:

    1. Add `ENABLE_ADAPTOR_MLP_V2 = False` to experiment variables section (per D-02, D-05). Add comment: "# Adaptor MLP v2: LayerNorm+GELU+residual projection (replaces simple linear when enabled)"

    2. Implement `class AdaptorMLPv2(nn.Module)` per D-05:
       - `__init__(self, in_features: int, out_features: int)`:
         - layer1: nn.Linear(in_features, out_features)
         - norm: nn.LayerNorm(out_features)
         - act: nn.GELU()
         - layer2: nn.Linear(out_features, out_features)
         - use_residual: bool = (in_features == out_features)
       - `forward(self, x) -> Tensor`:
         - h = act(norm(layer1(x)))
         - h = layer2(h)
         - if use_residual: h = h + x
         - return h

    Per D-05: Agent can toggle between simple ProjectionHead and AdaptorMLPv2 per teacher. The actual swapping logic is handled in Plan 03 integration. This task only defines the class.

    Place after FeatureNormalizer class.
  </action>
  <verify>
    <automated>cd /home/whiskey/workspace/project/central/v2/training/autoresearch && python -c "
import torch
from train import AdaptorMLPv2, ENABLE_ADAPTOR_MLP_V2

# Flag check
assert ENABLE_ADAPTOR_MLP_V2 == False

# Different dims (no residual)
mlp = AdaptorMLPv2(768, 256)
x = torch.randn(8, 768, requires_grad=True)
out = mlp(x)
assert out.shape == (8, 256), f'Expected (8,256), got {out.shape}'
out.sum().backward()
assert x.grad is not None, 'Gradients should flow'

# Same dims (residual)
mlp2 = AdaptorMLPv2(256, 256)
x2 = torch.randn(8, 256)
out2 = mlp2(x2)
assert out2.shape == (8, 256)
assert not torch.equal(out2, x2), 'Output should differ from input'

print('AdaptorMLPv2: ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ENABLE_ADAPTOR_MLP_V2 = False" train.py
    - grep -q "class AdaptorMLPv2" train.py
    - grep -q "nn.LayerNorm" train.py
    - grep -q "nn.GELU" train.py
    - grep -q "use_residual" train.py
  </acceptance_criteria>
  <done>AdaptorMLPv2 class exists in train.py with 2-layer MLP, LayerNorm, GELU, and conditional residual. ENABLE_ADAPTOR_MLP_V2 flag defaults to False.</done>
</task>

</tasks>

<verification>
All three feature processing modules importable and functional:
```bash
python -c "from train import PHISTransform, FeatureNormalizer, AdaptorMLPv2, ENABLE_PHI_S, ENABLE_FEATURE_NORMALIZER, ENABLE_ADAPTOR_MLP_V2; print('All modules importable')"
```

All ENABLE flags default to False (no regression on existing behavior):
```bash
python -c "from train import ENABLE_PHI_S, ENABLE_FEATURE_NORMALIZER, ENABLE_ADAPTOR_MLP_V2; assert not any([ENABLE_PHI_S, ENABLE_FEATURE_NORMALIZER, ENABLE_ADAPTOR_MLP_V2]); print('All flags False')"
```
</verification>

<success_criteria>
- PHISTransform, FeatureNormalizer, AdaptorMLPv2 classes defined in train.py
- _build_hadamard utility function defined in train.py
- ENABLE_PHI_S, ENABLE_FEATURE_NORMALIZER, ENABLE_ADAPTOR_MLP_V2 module-level constants default to False
- NORMALIZER_WARMUP_BATCHES = 200 module-level constant exists
- All smoke tests pass (import + forward pass + gradient flow)
- Existing train.py functionality unchanged (flags default off)
</success_criteria>

<output>
After completion, create `.planning/phases/09-radio-training-techniques-wrap-up/09-01-SUMMARY.md`
</output>
