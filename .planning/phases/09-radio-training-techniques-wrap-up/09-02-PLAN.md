---
phase: 09-radio-training-techniques-wrap-up
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - train.py
autonomous: true
requirements:
  - TRAIN-03
  - TRAIN-04
  - TRAIN-06
  - TRAIN-07

must_haves:
  truths:
    - "L_angle loss normalizes angular error by teacher angular dispersion to prevent imbalanced gradients"
    - "Hybrid loss combines cosine similarity and smooth-L1 for spatial feature distillation"
    - "FeatSharp sharpens spatial features via attention-weighted refinement (optional, gated)"
    - "Shift equivariant loss applies random shifts to spatial features before MSE (optional, gated)"
    - "All four loss/technique functions are gated by independent ENABLE_* flags defaulting to False"
  artifacts:
    - path: "train.py"
      provides: "l_angle_loss, hybrid_loss, shift_equivariant_loss functions + FeatSharpModule class"
      contains: "def l_angle_loss"
    - path: "train.py"
      provides: "ENABLE flags for all four techniques"
      contains: "ENABLE_L_ANGLE"
  key_links:
    - from: "l_angle_loss"
      to: "distillation loss computation"
      via: "replaces cosine loss when enabled"
      pattern: "l_angle_loss"
    - from: "hybrid_loss"
      to: "spatial distillation"
      via: "replaces MSE for spatial features when enabled"
      pattern: "hybrid_loss"
    - from: "shift_equivariant_loss"
      to: "spatial distillation"
      via: "adds random shift augmentation to spatial MSE"
      pattern: "shift_equivariant_loss"
---

<objective>
Implement four RADIO-inspired loss functions and spatial techniques in train.py: L_angle (balanced summary loss), Hybrid Loss (cosine+smooth-L1), FeatSharp (spatial sharpening), and Shift Equivariant Loss (shifted spatial MSE).

Purpose: These loss functions improve distillation quality by balancing teacher contributions (L_angle), combining direction and magnitude signals (Hybrid Loss), sharpening spatial features (FeatSharp), and preventing positional noise learning (Shift Equivariant).
Output: Loss functions + ENABLE_* flags in train.py. Not yet wired into training loop (Plan 03 handles integration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-radio-training-techniques-wrap-up/09-CONTEXT.md
@.planning/phases/09-radio-training-techniques-wrap-up/09-RESEARCH.md
@train.py

<interfaces>
<!-- Existing loss function pattern in train.py -->

From train.py (line ~303):
```python
def vat_embedding_loss(
    model: "FrozenBackboneWithHead",
    images: torch.Tensor,
    embeddings: torch.Tensor,
    epsilon: float = 8.0,
) -> torch.Tensor:
    # ... existing loss function pattern ...
```

From prepare.py (imported):
```python
EMBEDDING_DIM = 256  # All embeddings are 256d L2-normalized
```

<!-- NOTE: Spatial features (TRAIN-06, TRAIN-07) depend on Phase 8 RADIO-04 spatial cache.
     These techniques must be gated on spatial feature availability.
     FeatSharp and Shift Equivariant Loss are OPTIONAL per D-01. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: L_angle and Hybrid Loss functions</name>
  <files>train.py</files>
  <read_first>
    - train.py (full file -- understand existing loss functions, experiment variables section)
    - .planning/phases/09-radio-training-techniques-wrap-up/09-RESEARCH.md (L_angle code example from C-RADIOv4 Section 2.5, Hybrid Loss from PHI-S paper Eq. 3, Pitfall 4 and 5)
  </read_first>
  <behavior>
    - l_angle_loss(student_256, teacher_256, mean_dir_256, dispersion=1.5) returns scalar tensor > 0
    - l_angle_loss with dispersion near 0 (0.01) still returns finite value (epsilon protection)
    - l_angle_loss with identical student/teacher returns ~0
    - hybrid_loss(student_256, teacher_256, beta=0.9) returns scalar tensor > 0
    - hybrid_loss with identical tensors returns ~0
    - hybrid_loss with beta=1.0 equals pure cosine loss
    - hybrid_loss with beta=0.0 equals pure smooth_l1_loss
  </behavior>
  <action>
    Add to train.py experiment variables section:

    1. `ENABLE_L_ANGLE = False` with comment "# L_angle: balanced summary loss normalized by angular dispersion (per D-03)"
    2. `ENABLE_HYBRID_LOSS = False` with comment "# Hybrid Loss: 0.9*cosine + 0.1*smooth-L1 for spatial features (per D-03)"
    3. `HYBRID_LOSS_BETA = 0.9` with comment "# Cosine weight in hybrid loss (1-beta = smooth-L1 weight)"

    Implement in the loss functions section of train.py (after vat_embedding_loss):

    4. `def l_angle_loss(student, teacher, teacher_mean_dir, teacher_angular_dispersion, eps=1e-6) -> Tensor`:
       Per C-RADIOv4 Section 2.5 Eq. 3-7:
       - Compute cosine similarity between student and teacher
       - Convert to angle: theta = arccos(clamp(cos_sim, -1+eps, 1-eps))
       - Return mean(theta^2) / (teacher_angular_dispersion + eps)
       - The teacher_mean_dir and teacher_angular_dispersion are pre-computed per teacher (by Plan 03 during training init). Add docstring explaining these must be pre-computed from the training set.

    5. `def hybrid_loss(student, teacher, beta=0.9) -> Tensor`:
       Per PHI-S paper Section 2.1 Eq. 3:
       - cos_loss = (1 - cosine_similarity(student, teacher, dim=-1)).mean()
       - sml1_loss = F.smooth_l1_loss(student, teacher)
       - return beta * cos_loss + (1 - beta) * sml1_loss
       - IMPORTANT per Pitfall 5: Hybrid loss is designed for SPATIAL features (patches), not summary features. For summary features, use cosine or L_angle. Add docstring noting this distinction.
  </action>
  <verify>
    <automated>cd /home/whiskey/workspace/project/central/v2/training/autoresearch && python -c "
import torch
import torch.nn.functional as F
from train import l_angle_loss, hybrid_loss, ENABLE_L_ANGLE, ENABLE_HYBRID_LOSS, HYBRID_LOSS_BETA

# Flags
assert ENABLE_L_ANGLE == False
assert ENABLE_HYBRID_LOSS == False
assert HYBRID_LOSS_BETA == 0.9

# L_angle basic
s = torch.randn(16, 256)
t = torch.randn(16, 256)
mean_dir = F.normalize(torch.randn(256), dim=0)
loss = l_angle_loss(s, t, mean_dir, 1.5)
assert loss.ndim == 0, 'Should be scalar'
assert loss.item() > 0, 'Should be positive for random vectors'

# L_angle with near-zero dispersion (epsilon protection)
loss2 = l_angle_loss(s, t, mean_dir, 0.01)
assert torch.isfinite(loss2), 'Should be finite even with small dispersion'

# L_angle identical inputs
s_same = F.normalize(torch.randn(16, 256), dim=1)
loss3 = l_angle_loss(s_same, s_same, mean_dir, 1.0)
assert loss3.item() < 0.01, f'Identical inputs should give ~0, got {loss3.item()}'

# Hybrid loss basic
loss4 = hybrid_loss(s, t, beta=0.9)
assert loss4.ndim == 0
assert loss4.item() > 0

# Hybrid loss identical
loss5 = hybrid_loss(s_same, s_same, beta=0.9)
assert loss5.item() < 0.01, f'Identical should give ~0, got {loss5.item()}'

# Hybrid beta=1.0 should be pure cosine
cos_only = hybrid_loss(s, t, beta=1.0)
manual_cos = (1 - F.cosine_similarity(s, t, dim=-1)).mean()
assert torch.allclose(cos_only, manual_cos, atol=1e-5), 'beta=1 should equal pure cosine'

print('L_angle + Hybrid Loss: ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ENABLE_L_ANGLE = False" train.py
    - grep -q "ENABLE_HYBRID_LOSS = False" train.py
    - grep -q "HYBRID_LOSS_BETA = 0.9" train.py
    - grep -q "def l_angle_loss" train.py
    - grep -q "def hybrid_loss" train.py
    - grep -q "torch.acos" train.py
    - grep -q "smooth_l1_loss" train.py
  </acceptance_criteria>
  <done>l_angle_loss and hybrid_loss functions exist in train.py. ENABLE_L_ANGLE, ENABLE_HYBRID_LOSS, HYBRID_LOSS_BETA flags exist. L_angle handles near-zero dispersion. Hybrid loss correctly combines cosine + smooth-L1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FeatSharp and Shift Equivariant Loss (spatial techniques)</name>
  <files>train.py</files>
  <read_first>
    - train.py (current state after Task 1)
    - .planning/phases/09-radio-training-techniques-wrap-up/09-RESEARCH.md (Shift Equivariant Loss code example from C-RADIOv4 Section 2.3.1, FeatSharp from ICML 2025, Pitfall 6)
    - RADIO/FeatSharp.pdf (Section 3.3 for tile-guided attentional refinement, if needed)
    - RADIO/RADIOv4.0_tech_report.pdf (Section 2.3.1 for shift equivariant formulation, if needed)
  </read_first>
  <behavior>
    - shift_equivariant_loss(student_4d, teacher_4d, max_shift=2) returns scalar tensor >= 0
    - shift_equivariant_loss with identical inputs returns 0 (MSE of identical regions)
    - shift_equivariant_loss handles case where overlap region is empty (returns 0)
    - FeatSharpModule(256, num_heads=4) forward pass: input (B,C,H,W) -> output (B,C,H,W) same shape
    - FeatSharpModule produces different output than input (not identity)
    - Gradients flow through FeatSharpModule
  </behavior>
  <action>
    Add to train.py experiment variables section:

    1. `ENABLE_FEATSHARP = False` with comment "# FeatSharp: spatial feature sharpening -- OPTIONAL, may OOM (per D-01)"
    2. `ENABLE_SHIFT_EQUIVARIANT = False` with comment "# Shift Equivariant Loss: random-shift spatial MSE -- OPTIONAL (per D-01)"
    3. `SHIFT_EQUIVARIANT_MAX_SHIFT = 2` with comment "# Max shift in patches for shift equivariant loss"

    Implement in the loss functions section:

    4. `def shift_equivariant_loss(student_spatial, teacher_spatial, max_shift=2) -> Tensor`:
       Per C-RADIOv4 Section 2.3.1 Eq. 1:
       - Generate random shifts for student and teacher independently: torch.randint(-max_shift, max_shift+1, (2,)) for each
       - Compute overlap region between shifted student and teacher
       - If overlap is empty (h_end <= h_start or w_end <= w_start), return 0
       - Extract overlapping regions from both, compute MSE loss
       - IMPORTANT per Pitfall 6: This requires spatial (patch-level) features. Gate on availability. Add docstring.

    5. `class FeatSharpModule(nn.Module)`:
       Per FeatSharp paper Section 3.3 -- simplified version for CNN spatial features:
       - Lightweight attention-based spatial refinement
       - `__init__(self, channels: int, num_heads: int = 4)`:
         - Multi-head attention layer (nn.MultiheadAttention)
         - LayerNorm for pre-attention normalization
         - Residual connection
       - `forward(self, spatial_features: Tensor) -> Tensor`:
         - Input: (B, C, H, W)
         - Reshape to (B, C, H*W) -> (H*W, B, C) for attention
         - Apply LN -> MHA (self-attention) -> residual
         - Reshape back to (B, C, H, W)
       - IMPORTANT: This is OPTIONAL per D-01. Default ENABLE_FEATSHARP=False. If OOM occurs, crash recovery handles it. Add docstring noting VRAM concern.

    Both techniques depend on Phase 8 spatial feature cache (RADIO-04). They will only activate when spatial features are available at runtime. Add clear docstrings explaining this dependency.
  </action>
  <verify>
    <automated>cd /home/whiskey/workspace/project/central/v2/training/autoresearch && python -c "
import torch
from train import shift_equivariant_loss, FeatSharpModule, ENABLE_FEATSHARP, ENABLE_SHIFT_EQUIVARIANT, SHIFT_EQUIVARIANT_MAX_SHIFT

# Flags
assert ENABLE_FEATSHARP == False
assert ENABLE_SHIFT_EQUIVARIANT == False
assert SHIFT_EQUIVARIANT_MAX_SHIFT == 2

# Shift equivariant loss basic
s = torch.randn(4, 256, 7, 7)
t = torch.randn(4, 256, 7, 7)
loss = shift_equivariant_loss(s, t, max_shift=2)
assert loss.ndim == 0, 'Should be scalar'
assert loss.item() >= 0, 'Should be non-negative'

# Identical inputs
loss2 = shift_equivariant_loss(s, s, max_shift=0)
assert loss2.item() < 1e-6, f'Identical with no shift should be ~0, got {loss2.item()}'

# FeatSharp basic
fs = FeatSharpModule(256, num_heads=4)
x = torch.randn(4, 256, 7, 7, requires_grad=True)
out = fs(x)
assert out.shape == (4, 256, 7, 7), f'Expected (4,256,7,7), got {out.shape}'
out.sum().backward()
assert x.grad is not None, 'Gradients should flow'

print('FeatSharp + Shift Equivariant: ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ENABLE_FEATSHARP = False" train.py
    - grep -q "ENABLE_SHIFT_EQUIVARIANT = False" train.py
    - grep -q "SHIFT_EQUIVARIANT_MAX_SHIFT = 2" train.py
    - grep -q "def shift_equivariant_loss" train.py
    - grep -q "class FeatSharpModule" train.py
    - grep -q "MultiheadAttention" train.py
  </acceptance_criteria>
  <done>shift_equivariant_loss function and FeatSharpModule class exist in train.py. ENABLE_FEATSHARP, ENABLE_SHIFT_EQUIVARIANT, SHIFT_EQUIVARIANT_MAX_SHIFT flags exist. Both properly gated and documented as optional/spatial-dependent.</done>
</task>

</tasks>

<verification>
All four loss functions/modules importable:
```bash
python -c "from train import l_angle_loss, hybrid_loss, shift_equivariant_loss, FeatSharpModule; print('All loss functions importable')"
```

All ENABLE flags default to False:
```bash
python -c "from train import ENABLE_L_ANGLE, ENABLE_HYBRID_LOSS, ENABLE_FEATSHARP, ENABLE_SHIFT_EQUIVARIANT; assert not any([ENABLE_L_ANGLE, ENABLE_HYBRID_LOSS, ENABLE_FEATSHARP, ENABLE_SHIFT_EQUIVARIANT]); print('All flags False')"
```
</verification>

<success_criteria>
- l_angle_loss, hybrid_loss, shift_equivariant_loss functions defined in train.py
- FeatSharpModule class defined in train.py
- ENABLE_L_ANGLE, ENABLE_HYBRID_LOSS, ENABLE_FEATSHARP, ENABLE_SHIFT_EQUIVARIANT flags default to False
- HYBRID_LOSS_BETA and SHIFT_EQUIVARIANT_MAX_SHIFT tunable constants exist
- All smoke tests pass
- Existing train.py functionality unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/09-radio-training-techniques-wrap-up/09-02-SUMMARY.md`
</output>
