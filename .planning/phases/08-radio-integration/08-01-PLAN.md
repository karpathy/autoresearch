---
phase: 08-radio-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prepare.py
autonomous: true
requirements:
  - RADIO-01
  - RADIO-02
  - RADIO-03
  - RADIO-06

must_haves:
  truths:
    - "RADIOTeacher loads C-RADIOv4-SO400M and C-RADIOv4-H models via torch.hub.load from local RADIO/ clone"
    - "RADIOTeacher extracts summary features from all 3 adaptors (backbone, dino_v3, siglip2-g)"
    - "Summary features are cached as per-sample .npy files in per-adaptor directories with native dimensions"
    - "RADIO_VARIANT and RADIO_ADAPTORS constants in train.py control which model/adaptors the agent distills from"
  artifacts:
    - path: "prepare.py"
      provides: "RADIOTeacher class with adaptor-aware summary caching"
      contains: "class RADIOTeacher"
    - path: "prepare.py"
      provides: "RADIO version map and load function"
      contains: "RADIO_VERSION_MAP"
  key_links:
    - from: "prepare.py RADIOTeacher"
      to: "RADIO/hubconf.py radio_model()"
      via: "torch.hub.load with source='local'"
      pattern: "torch\\.hub\\.load.*RADIO.*radio_model"
    - from: "prepare.py RADIOTeacher.encode_batch"
      to: "workspace/output/teacher_cache/radio_*/"
      via: "np.save per-sample .npy files"
      pattern: "np\\.save.*\\.npy"
---

<objective>
Add RADIOTeacher class to prepare.py that loads C-RADIOv4 models (SO400M and H variants) with adaptor selection, extracts summary features from all 3 adaptors (backbone, dino_v3, siglip2-g), and caches them as per-sample .npy files following the existing teacher cache pattern.

Purpose: Enable RADIO models as distillation teachers in the autoresearch pipeline, giving the agent access to 3 distinct adaptor outputs per RADIO variant.
Output: RADIOTeacher class in prepare.py, summary caches in workspace/output/teacher_cache/radio_*/{adaptor}/, and RADIO_VARIANT/RADIO_ADAPTORS constants wired in train.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-radio-integration/08-CONTEXT.md
@.planning/phases/08-radio-integration/08-RESEARCH.md

@prepare.py
@train.py
@RADIO/hubconf.py
@RADIO/test_example.py

<interfaces>
<!-- Existing teacher pattern in prepare.py -->
From prepare.py (line 215):
```python
class DINOv2Teacher:
    def __init__(self, model_name: str = "...", device: str = "cuda") -> None:
        ...
    @torch.no_grad()
    def encode_batch(self, images: list[np.ndarray | Image.Image]) -> list[np.ndarray | None]:
        ...
```

From prepare.py (line 664):
```python
def load_teacher_embeddings(
    image_paths: Sequence[str],
    teacher: TrendyolEmbedder,
    device: torch.device,
    cache_dir: str | None = None,
) -> torch.Tensor:
    """Load teacher embeddings with in-memory + disk caching and batch inference."""
```

From train.py (lines 49-70):
```python
TEACHER_CACHE_DIR = DEFAULT_TEACHER_CACHE_DIR
```

From RADIO/hubconf.py (line 30):
```python
def radio_model(
    version: str = "",
    progress: bool = True,
    adaptor_names: Union[str, List[str]] = None,
    vitdet_window_size: Optional[int] = None,
    return_checkpoint: bool = False,
    **kwargs,
) -> RADIOModel:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RADIOTeacher class to prepare.py with adaptor-aware summary caching</name>
  <read_first>
    - prepare.py (full file -- understand existing DINOv2Teacher pattern, load_teacher_embeddings, TrendyolEmbedder, cache dir structure)
    - RADIO/hubconf.py (understand radio_model() loading API, version strings, adaptor_names param)
    - RADIO/radio/radio_model.py (understand RADIOModel forward, summary/features output, RadioOutput namedtuple)
    - RADIO/radio/common.py (RESOURCE_MAP keys for c-radio_v4-so400m and c-radio_v4-h)
    - RADIO/radio/adaptor_base.py (RadioOutput namedtuple: summary + features fields)
    - RADIO/test_example.py (official usage example for reference)
  </read_first>
  <files>prepare.py</files>
  <action>
    Add a RADIOTeacher class to prepare.py (after DINOv2Teacher, before DistillImageFolder). This implements RADIO-01, RADIO-02, RADIO-03 per user decisions D-01 through D-05, D-09 through D-11.

    1. Add RADIO version map constant at module level:
       ```python
       RADIO_VERSION_MAP = {
           "so400m": "c-radio_v4-so400m",
           "h": "c-radio_v4-h",
       }
       ```

    2. Create RADIOTeacher class with:
       - `__init__(self, variant: str, adaptor_names: list[str], device: str = "cuda")`:
         - Load model via `torch.hub.load("RADIO", "radio_model", source="local", version=RADIO_VERSION_MAP[variant], adaptor_names=adaptor_names, progress=True, skip_validation=True)`
         - Move to device, eval(), freeze all params
         - Run a dummy forward pass with a [1, 3, 224, 224] tensor in [0, 1] range to determine feature dims
         - Store `self.feature_dims` dict mapping adaptor name -> {'summary_dim': int, 'spatial_dim': int, 'spatial_tokens': int}
         - Also store backbone entry in feature_dims from the backbone output
         - Log variant, adaptor names, and discovered dims
         - CRITICAL: Images must be in [0, 1] range (NOT ImageNet-normalized). RADIO has its own InputConditioner that applies CLIP normalization internally.
         - CRITICAL: Backbone summary dim will be embed_dim * num_summary_idxs (e.g., 3456 for SO400M with 3 CLS tokens, NOT 1152). Always determine from dummy pass, never hardcode.

       - `get_feature_dim(self, adaptor: str = "backbone") -> int`:
         - Return self.feature_dims[adaptor]['summary_dim']

       - `@torch.no_grad() encode_batch(self, images: list[np.ndarray | Image.Image], adaptor: str = "backbone") -> list[np.ndarray | None]`:
         - Convert PIL/ndarray images to tensors in [0, 1] range: `pil_to_tensor(img).float().div_(255.0)`
         - Use PadToSquare() then resize to (224, 224) BEFORE converting to tensor
         - Call `model.get_nearest_supported_resolution(*batch.shape[-2:])` and interpolate if needed
         - Run model with `torch.autocast('cuda', dtype=torch.bfloat16)`
         - Extract the requested adaptor's summary from the output dict
         - Return list of numpy arrays (float32)

       - `@torch.no_grad() encode_batch_all_adaptors(self, images: list[np.ndarray | Image.Image]) -> dict[str, list[np.ndarray | None]]`:
         - Same as encode_batch but returns ALL loaded adaptors' summaries in one forward pass
         - Returns dict mapping adaptor_name -> list of numpy summary arrays
         - This avoids running RADIO multiple times when caching all adaptors (per D-01)

    3. Add `build_radio_summary_cache` function:
       - Parameters: `variant: str, adaptor_names: list[str], image_paths: list[str], cache_base: str, batch_size: int = 32, device: str = "cuda"`
       - Creates RADIOTeacher instance
       - For each adaptor in adaptor_names, create cache directory: `{cache_base}/radio_{variant}/{adaptor}/`
       - Write `metadata.json` in each adaptor dir with: model_version, adaptor_name, summary_dim, spatial_dim, spatial_tokens, num_samples, cache_date
       - Process images in batches using encode_batch_all_adaptors
       - Save per-sample summary as `{md5_hash}.npy` (following existing pattern from load_teacher_embeddings)
       - Per D-10: cache dirs follow `workspace/output/teacher_cache/radio_so400m/backbone/` pattern
       - Skip already-cached samples (check if .npy exists)
       - Log progress every 1000 samples

    4. Add `load_radio_teacher_embeddings` function:
       - Parameters: `image_paths: Sequence[str], adaptor: str, cache_dir: str, device: torch.device`
       - Follows same pattern as existing `load_teacher_embeddings` but reads from RADIO cache dir
       - Uses _TEACHER_MEM_CACHE with key = f"radio:{adaptor}:{path}" to avoid collisions
       - Returns torch.Tensor of stacked embeddings

    5. Export the new symbols so train.py can import them:
       - RADIOTeacher, RADIO_VERSION_MAP, build_radio_summary_cache, load_radio_teacher_embeddings
  </action>
  <verify>
    <automated>python -c "
import sys; sys.path.insert(0, '.')
from prepare import RADIOTeacher, RADIO_VERSION_MAP, build_radio_summary_cache, load_radio_teacher_embeddings
assert 'so400m' in RADIO_VERSION_MAP
assert 'h' in RADIO_VERSION_MAP
print('Imports OK')
t = RADIOTeacher('so400m', ['backbone'], device='cuda')
dim = t.get_feature_dim('backbone')
print(f'backbone summary_dim={dim}')
assert dim > 0
print('RADIOTeacher loads and reports dims')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class RADIOTeacher" prepare.py
    - grep -q "RADIO_VERSION_MAP" prepare.py
    - grep -q "def build_radio_summary_cache" prepare.py
    - grep -q "def load_radio_teacher_embeddings" prepare.py
    - grep -q "torch.hub.load" prepare.py
    - grep -q "source.*local" prepare.py
    - grep -q "div_.*255" prepare.py (images in [0,1] range)
    - grep -q "metadata.json" prepare.py (per-adaptor metadata)
    - grep -q "encode_batch_all_adaptors" prepare.py
  </acceptance_criteria>
  <done>RADIOTeacher class loads C-RADIOv4 models from local RADIO/ clone, extracts summary features from all requested adaptors in a single forward pass, caches per-sample .npy summaries in per-adaptor directories with metadata.json, and exports load function for train.py consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Wire RADIO constants and summary loading into train.py</name>
  <read_first>
    - train.py (full file -- understand existing TEACHER_CACHE_DIR, distillation loss, training loop, how teacher embeddings are loaded)
    - prepare.py (after Task 1 -- understand new exports: RADIOTeacher, load_radio_teacher_embeddings, RADIO_VERSION_MAP)
  </read_first>
  <files>train.py</files>
  <action>
    Add RADIO-specific constants and summary distillation wiring to train.py. This implements RADIO-06 and completes the summary distillation path for RADIO-03.

    1. Add module-level constants (per D-04, D-05):
       ```python
       # RADIO teacher configuration
       RADIO_VARIANT = "so400m"              # "so400m" or "h"
       RADIO_ADAPTORS = ["backbone"]          # subset of ["backbone", "dino_v3", "siglip2-g"]
       RADIO_CACHE_BASE = "workspace/output/teacher_cache"
       ```

    2. Add imports from prepare.py:
       ```python
       from prepare import load_radio_teacher_embeddings, RADIO_VERSION_MAP
       ```

    3. Add per-adaptor projection heads (per D-05):
       - In the model class or as separate module dict, create a `nn.ModuleDict` keyed by adaptor name
       - Each projection: `nn.Sequential(nn.Linear(adaptor_summary_dim, EMBEDDING_DIM))` (simple linear, agent can tune later)
       - Dims are read from the cached metadata.json at train startup
       - Example: `RADIO_PROJ_HEADS = nn.ModuleDict({"backbone": nn.Linear(3456, 256), ...})`

    4. Wire RADIO summary loading into the training loop:
       - When RADIO is the active teacher (check if TEACHER_CACHE_DIR points to a radio cache, or add a USE_RADIO flag):
         - Load teacher embeddings via `load_radio_teacher_embeddings(paths, adaptor, cache_dir, device)`
         - Project through per-adaptor projection head
         - Compute distillation loss (cosine similarity, same as existing teacher)
       - Support multiple adaptors: if RADIO_ADAPTORS has multiple entries, compute weighted loss across all

    5. Add a helper to read adaptor metadata at startup:
       ```python
       def _load_radio_metadata(variant, adaptors, cache_base):
           import json
           dims = {}
           for adaptor in adaptors:
               meta_path = Path(cache_base) / f"radio_{variant}" / adaptor / "metadata.json"
               with open(meta_path) as f:
                   dims[adaptor] = json.load(f)["summary_dim"]
           return dims
       ```

    NOTE: This task adds summary-only distillation from RADIO. Spatial distillation is Plan 02.
  </action>
  <verify>
    <automated>python -c "
import ast, sys
with open('train.py') as f:
    source = f.read()
tree = ast.parse(source)
names = [n.id for n in ast.walk(tree) if isinstance(n, ast.Name)]
assert 'RADIO_VARIANT' in source, 'RADIO_VARIANT constant missing'
assert 'RADIO_ADAPTORS' in source, 'RADIO_ADAPTORS constant missing'
assert 'load_radio_teacher_embeddings' in source, 'RADIO loading import missing'
print('Constants and imports present')
# Verify no hardcoded dims
assert '3456' not in source or 'metadata' in source, 'Hardcoded dim without metadata fallback'
print('No hardcoded dims')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RADIO_VARIANT" train.py
    - grep -q "RADIO_ADAPTORS" train.py
    - grep -q "load_radio_teacher_embeddings" train.py
    - grep -q "metadata.json\|_load_radio_metadata" train.py (reads dims from metadata)
    - grep -q "ModuleDict\|proj.*radio\|projection.*radio" train.py (per-adaptor projection heads, case insensitive)
  </acceptance_criteria>
  <done>train.py has RADIO_VARIANT and RADIO_ADAPTORS as module-level constants the agent can tune, per-adaptor projection heads with dims read from cached metadata, and RADIO summary embeddings loaded and used in distillation loss.</done>
</task>

</tasks>

<verification>
1. `python -c "from prepare import RADIOTeacher; t = RADIOTeacher('so400m', ['backbone']); print(t.get_feature_dim('backbone'))"` -- loads model and reports summary dim
2. `grep -c "class RADIOTeacher" prepare.py` returns 1
3. `grep "RADIO_VARIANT\|RADIO_ADAPTORS" train.py` shows both constants
4. `python -c "from prepare import RADIO_VERSION_MAP; assert set(RADIO_VERSION_MAP.keys()) == {'so400m', 'h'}"` -- version map correct
</verification>

<success_criteria>
- RADIOTeacher class exists in prepare.py and can load both SO400M and H variants
- All 3 adaptors (backbone, dino_v3, siglip2-g) produce summary embeddings
- Summary features are cached as per-sample .npy in per-adaptor directories
- metadata.json in each adaptor dir records summary_dim, spatial_dim, spatial_tokens
- train.py has RADIO_VARIANT and RADIO_ADAPTORS as agent-tunable constants
- Per-adaptor projection heads in train.py use dims from metadata (no hardcoding)
- Images passed to RADIO are in [0,1] range (not ImageNet-normalized)
</success_criteria>

<output>
After completion, create `.planning/phases/08-radio-integration/08-01-SUMMARY.md`
</output>
