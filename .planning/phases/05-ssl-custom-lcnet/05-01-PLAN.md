---
phase: 05-ssl-custom-lcnet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - train.py
  - pyproject.toml
  - tests/test_train.py
autonomous: true
requirements: [LCNET-01, LCNET-02, LCNET-03, LCNET-04, INFRA-09]

must_haves:
  truths:
    - "Custom LCNet class exists in train.py and .encode() returns L2-normalized Tensor[B, 256]"
    - "forward_features() returns (spatial: Tensor[B, C, H, W], summary: Tensor[B, 1280])"
    - "encode_with_spatial() returns (embedding: Tensor[B, 256], spatial: Tensor[B, C, H, W])"
    - "All tunable constants exist at module level: LCNET_SCALE, SE_START_BLOCK, SE_REDUCTION, ACTIVATION, KERNEL_SIZES, USE_PRETRAINED"
    - "Pretrained weight loading works for LCNET_SCALE=0.5 (lcnet_050)"
    - "einops is in pyproject.toml dependencies"
  artifacts:
    - path: "train.py"
      provides: "Custom LCNet backbone replacing FrozenBackboneWithHead"
      contains: "class LCNet"
    - path: "train.py"
      provides: "DepthwiseSeparableConv and SqueezeExcite blocks"
      contains: "class DepthwiseSeparableConv"
    - path: "pyproject.toml"
      provides: "einops dependency"
      contains: "einops"
    - path: "tests/test_train.py"
      provides: "Tests for LCNet requirements"
      contains: "test_forward_features_spatial"
  key_links:
    - from: "train.py::LCNet.encode()"
      to: "prepare.py::run_retrieval_eval()"
      via: ".encode() contract returning L2-normalized Tensor[B, 256]"
      pattern: "def encode.*Tensor\\[B, 256\\]|functional\\.normalize.*p=2.*dim=1"
    - from: "train.py::main()"
      to: "train.py::LCNet"
      via: "model instantiation replaces FrozenBackboneWithHead"
      pattern: "model = LCNet\\("
---

<objective>
Replace the timm-wrapped FrozenBackboneWithHead with a fully custom LCNet backbone built from nn.Module primitives, exposing agent-tunable architecture parameters and spatial feature APIs needed for future RADIO distillation. Add einops to pyproject.toml.

Purpose: Give the agent full control over backbone architecture (width, SE placement, kernels, activation) while preserving the .encode() contract with prepare.py. The spatial feature API prepares for Phase 8 RADIO spatial distillation.
Output: Custom LCNet class in train.py with all APIs, pretrained weight loading, tunable constants, einops dependency, and updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ssl-custom-lcnet/05-CONTEXT.md
@.planning/phases/05-ssl-custom-lcnet/05-RESEARCH.md
@train.py
@pyproject.toml
@tests/test_train.py
</context>

<interfaces>
<!-- Existing contract that MUST be preserved -->
From prepare.py (DO NOT MODIFY):
- run_retrieval_eval(model, ...) calls model.encode(images) expecting Tensor[B, 256]
- compute_combined_metric(recall_at_1, mean_cosine) unchanged
- EMBEDDING_DIM = 256, IMAGE_SIZE = 224, EPOCHS = 10

From train.py (current model to REPLACE):
```python
class FrozenBackboneWithHead(nn.Module):
    def encode(self, images: torch.Tensor) -> torch.Tensor:  # returns L2-normed [B, 256]
    def forward_embeddings_train(self, images: torch.Tensor) -> torch.Tensor:  # returns L2-normed [B, 256]
    def unfreeze_last_stage(self) -> None:
```

From train.py (existing loss and training loop integration points):
```python
# Line 471: loss = distill_loss + arc_loss_weight * arc_loss + sep_weight * l_sep
# Line 409: student_emb = model.forward_embeddings_train(images)
# Line 564: model = FrozenBackboneWithHead(model_name=MODEL_NAME, ...)
# Line 591-592: backbone_params = [p for p in model.backbone.parameters() if ...]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement custom LCNet backbone with all APIs and tunable constants</name>
  <files>train.py, pyproject.toml</files>
  <read_first>
    - train.py (full file -- understand current model, constants, training loop, main())
    - pyproject.toml (current dependencies)
    - .planning/phases/05-ssl-custom-lcnet/05-RESEARCH.md (architecture details, patterns, pitfalls)
    - .planning/phases/05-ssl-custom-lcnet/05-CONTEXT.md (locked decisions D-06 through D-13)
  </read_first>
  <action>
**A. Add tunable constants** (per D-07, D-09) to the EXPERIMENT VARIABLES section of train.py, right after the existing constants:

```python
# --- Custom LCNet Backbone ---
LCNET_SCALE = 0.5             # Width multiplier (0.5 matches current lcnet_050)
SE_START_BLOCK = 10           # Block index where SE begins (0-indexed, 13 total blocks)
SE_REDUCTION = 0.25           # SE squeeze ratio
ACTIVATION = "h_swish"        # "h_swish" | "relu" | "gelu"
KERNEL_SIZES = [3, 3, 3, 3, 3, 3, 5, 5, 5, 5, 5, 5, 5]  # Per-block kernel sizes (13 blocks)
USE_PRETRAINED = True          # Load timm pretrained weights when scale matches
```

**B. Implement helper classes** in the MODEL section, replacing FrozenBackboneWithHead:

1. `make_divisible(v, divisor=8, min_value=None)` -- channel rounding utility matching timm exactly. Formula: `max(min_value or divisor, int(v + divisor / 2) // divisor * divisor)` with the 0.9*v guard.

2. `SqueezeExcite(nn.Module)` -- SE block per RESEARCH Pattern 2:
   - `__init__(self, channels, reduced_ch)`: AdaptiveAvgPool2d(1), Conv2d(channels, reduced_ch, 1, bias=True), ReLU(inplace=True), Conv2d(reduced_ch, channels, 1, bias=True), Hardsigmoid gate.
   - `forward(self, x)`: pool -> reduce -> act -> expand -> gate -> x * scale.

3. `DepthwiseSeparableConv(nn.Module)` -- DSConv block per RESEARCH Pattern 1:
   - `__init__(self, in_ch, out_ch, kernel_size=3, stride=1, use_se=False, se_ratio=0.25, act_layer=nn.Hardswish)`:
     - Depthwise: Conv2d(in_ch, in_ch, kernel_size, stride, padding=kernel_size//2, groups=in_ch, bias=False) + BN + act
     - SE: SqueezeExcite if use_se, else nn.Identity()
     - Pointwise: Conv2d(in_ch, out_ch, 1, 1, 0, bias=False) + BN + act
   - `forward(self, x)`: depthwise -> bn1 -> act1 -> se -> pointwise -> bn2 -> act2

4. `LCNet(nn.Module)` -- full backbone per D-06, D-08, D-10, D-11, D-12, D-13:
   - `__init__(self, scale=LCNET_SCALE, se_start_block=SE_START_BLOCK, se_reduction=SE_REDUCTION, activation=ACTIVATION, kernel_sizes=KERNEL_SIZES, embedding_dim=256, device="cuda")`:
     - Activation lookup dict: `{"h_swish": nn.Hardswish, "relu": nn.ReLU, "gelu": nn.GELU}`
     - Architecture config table (LCNET_ARCH from RESEARCH) with base channels [16, 32, 64, 128, 256, 256, 512] for [stem, s0, s1, s2, s3, s4, s5]
     - Build stem: Conv2d(3, make_divisible(16*scale), 3, stride=2, padding=1, bias=False) + BN + activation. Use attribute name `conv_stem` and `bn1` to match timm naming.
     - Build blocks: nn.Sequential named `blocks` containing stages. Each stage is nn.Sequential. Use naming `blocks.{stage_idx}.{block_idx}` so that nested modules get timm-compatible keys. Track a flat block_idx counter starting at 0 to compare against se_start_block.
     - Build conv_head: Conv2d(last_ch, 1280, 1) + activation (no BN per paper). Attribute name `conv_head`.
     - Build head: `num_features = 1280`. GAP as AdaptiveAvgPool2d(1). Linear(1280, embedding_dim) as `self.embed_fc`. BatchNorm1d(embedding_dim) as `self.embed_bn`.
     - Weight init: kaiming_normal_ for Conv2d, xavier_uniform_ for Linear, ones/zeros for BN.

   - `forward_features(self, x) -> tuple[Tensor, Tensor]` (per D-11):
     - x through stem (conv_stem -> bn1 -> act)
     - x through all blocks (stages)
     - spatial = x (this is the pre-GAP feature map, [B, C, H, W])
     - x through conv_head + activation
     - x through GAP -> flatten
     - summary = x ([B, 1280])
     - return (spatial, summary)

   - `encode(self, images) -> Tensor[B, 256]` (per D-12):
     - self.eval(), torch.no_grad()
     - spatial, summary = self.forward_features(images)
     - emb = self.embed_bn(self.embed_fc(summary))
     - return F.normalize(emb, p=2, dim=1)

   - `forward_embeddings_train(self, images) -> Tensor[B, 256]`:
     - Same as encode but WITH gradients (no torch.no_grad, no self.eval)
     - spatial, summary = self.forward_features(images)
     - emb = self.embed_bn(self.embed_fc(summary))
     - return F.normalize(emb, p=2, dim=1)

   - `encode_with_spatial(self, images) -> tuple[Tensor, Tensor]` (per D-13):
     - self.eval(), torch.no_grad()
     - spatial, summary = self.forward_features(images)
     - emb = self.embed_bn(self.embed_fc(summary))
     - emb = F.normalize(emb, p=2, dim=1)
     - return (emb, spatial)

   - `unfreeze_last_stage(self) -> None`:
     - Unfreeze the last stage (blocks[-1]) and conv_head. All params require_grad = True.
     - Since custom LCNet starts fully trainable (no frozen backbone), this is kept for API compat but the default behavior changes: all params are trainable from start.

5. `load_pretrained_lcnet(model, scale)` -- per D-09, RESEARCH Pattern 6:
   - Map scale to variant: {0.5: 'lcnet_050', 0.75: 'lcnet_075', 1.0: 'lcnet_100'}
   - If scale not in map, log warning and return
   - Create timm model: `timm.create_model(f"hf-hub:timm/{variant}.ra2_in1k", pretrained=True, num_classes=0)`
   - Match keys by name and shape, load with strict=False
   - Log how many weights loaded
   - Delete timm model to free memory

**C. Update main() function:**

Replace `model = FrozenBackboneWithHead(...)` with:
```python
model = LCNet(
    scale=LCNET_SCALE,
    se_start_block=SE_START_BLOCK,
    se_reduction=SE_REDUCTION,
    activation=ACTIVATION,
    kernel_sizes=KERNEL_SIZES,
    embedding_dim=EMBEDDING_DIM,
    device=str(device),
).to(device)

if USE_PRETRAINED:
    load_pretrained_lcnet(model, LCNET_SCALE)
```

Update the optimizer parameter group construction. Since LCNet is fully custom (not frozen), all parameters are trainable. Use the same differential LR pattern but with LCNet's block groups:
- `head_params`: embed_fc, embed_bn, conv_head parameters
- `backbone_params`: conv_stem, bn1, blocks parameters
- If arc_margin, add to head_params

The `build_train_transform` currently creates a temporary timm model to get mean/std. Since we still use timm for pretrained weights (and the pretrained weights expect ImageNet normalization), keep using timm's resolve_data_config for normalization values. The MODEL_NAME constant can remain for this purpose, or hardcode ImageNet mean/std = (0.485, 0.456, 0.406), (0.229, 0.224, 0.225).

The backbone unfreezing logic (UNFREEZE_EPOCH) still applies -- at epoch 0 (default), call `model.unfreeze_last_stage()`. But since all params are already trainable in custom LCNet, `unfreeze_last_stage()` should be a no-op or selectively unfreeze only the last stage while freezing earlier stages initially.

**IMPORTANT CHANGE**: For the custom LCNet, rethink the freeze/unfreeze pattern. The current FrozenBackboneWithHead freezes ALL backbone params at init, then unfreezes last 4 stages at UNFREEZE_EPOCH. For the custom LCNet, do the same:
- In `__init__`, freeze ALL conv_stem, bn1, and blocks parameters (requires_grad=False)
- `unfreeze_last_stage()`: unfreeze blocks[-1], blocks[-2] (last 2 stages), and conv_head
- At UNFREEZE_EPOCH=0 (default), this happens immediately

This preserves the existing training dynamics where the backbone starts frozen and gets selectively unfrozen.

Actually, re-reading the current code more carefully: `UNFREEZE_EPOCH = 0` means unfreeze from start. And `unfreeze_last_stage` unfreezes the last 4 stages (indices -1, -2, -3, -4). Keep this same behavior for custom LCNet: freeze all backbone at init, unfreeze last N stages at UNFREEZE_EPOCH.

Update VAT loss function `vat_embedding_loss` -- it references `model.backbone(x)` and `model.proj(feat_clean)`. These need to work with the new LCNet. Options:
- Make LCNet have a `backbone` attribute that returns features (the stages part)
- Or refactor vat_embedding_loss to use forward_features

The cleanest approach: keep `model.backbone` as a property or attribute that encompasses conv_stem + bn1 + blocks (everything before conv_head/GAP/embed). This way VAT and the optimizer param grouping work with minimal changes.

**Approach for backbone compat:** Create a `nn.Sequential` wrapper:
```python
self.backbone = nn.Sequential(
    self.conv_stem, self.bn1, self.stem_act,
    self.blocks,
)
```
But this loses the named sub-modules. Better: create the stem and blocks as direct attributes, and have backbone be a method or a ModuleList that the VAT function can call.

**Simplest approach:** Rename things so `model.backbone(x)` works for VAT. Create `self.backbone` as an `nn.Module` that wraps stem + blocks (a simple forward-only wrapper). Then `model.proj` maps to the embed_fc + embed_bn. This maintains API compatibility with existing VAT code.

Actually, looking at VAT more carefully:
```python
feat_clean = model.backbone(x)   # needs to return features before projection
emb_clean = functional.normalize(model.proj(feat_clean), p=2, dim=1)  # proj + normalize
```

So we need:
- `model.backbone(x)` -> features (before projection head)
- `model.proj(features)` -> unnormalized embeddings

For custom LCNet: backbone = stem + blocks + conv_head + GAP (returns [B, 1280]), and proj = embed_fc + embed_bn (returns [B, 256]).

Make `self.proj = nn.Sequential(self.embed_fc, self.embed_bn)` -- but embed_fc is a Linear and embed_bn is a BatchNorm1d, so this works in Sequential.

Structure:
```python
# In LCNet.__init__:
self.features = nn.ModuleDict({...})  # stem, blocks, conv_head, etc.
self.proj = nn.Sequential(nn.Linear(1280, embedding_dim), nn.BatchNorm1d(embedding_dim))
```

And then define:
```python
def backbone(self, x):
    # stem
    x = self.stem_act(self.bn1(self.conv_stem(x)))
    # blocks
    x = self.blocks(x)
    # conv_head
    x = self.head_act(self.conv_head(x))
    # GAP
    x = self.gap(x).flatten(1)
    return x
```

Wait -- but we also need `forward_features` to return the PRE-conv_head spatial map. Let me make backbone NOT include conv_head, and handle it separately.

**FINAL STRUCTURE for LCNet:**

Attributes:
- `self.conv_stem`, `self.bn1`, `self.stem_act` -- stem
- `self.blocks` -- nn.Sequential of stages (each stage is nn.Sequential of DSConv blocks)
- `self.conv_head`, `self.head_act` -- 1x1 conv head
- `self.gap` -- AdaptiveAvgPool2d(1)
- `self.proj` -- nn.Sequential(Linear(1280, 256), BatchNorm1d(256))

Methods:
- `_forward_stem_blocks(self, x)` -> x after stem + all blocks (spatial features here)
- `forward_features(self, x)` -> (spatial, summary): spatial = _forward_stem_blocks output, then conv_head + gap + flatten for summary
- `forward_embeddings_train(self, x)` -> L2-normed [B, 256]: calls forward_features, then proj, then normalize
- `encode(self, x)` -> L2-normed [B, 256] (no grad)
- `encode_with_spatial(self, x)` -> (embedding, spatial) (no grad)

For VAT compatibility, we need `model.backbone(x)` and `model.proj(x)`. Add:
- `self.backbone` -- a small wrapper Module whose forward does _forward_stem_blocks + conv_head + gap + flatten (returns [B, 1280])
- OR just make LCNet work with the VAT function directly by updating VAT to use the model's API.

**Decision:** Update VAT to use forward_features instead of backbone/proj. This is cleaner:
```python
def vat_embedding_loss(model, x, ...):
    with torch.no_grad():
        spatial, feat_clean = model.forward_features(x)
        emb_clean = F.normalize(model.proj(feat_clean), p=2, dim=1)
    # ... rest uses model.proj(feat_clean + perturbation)
```

This requires `model.proj` to exist, which it does (nn.Sequential of Linear+BN).

**D. Add einops to pyproject.toml** (per D-14):
Add `"einops>=0.8.0",` to the dependencies list.

**E. Remove FrozenBackboneWithHead class** from train.py (replaced by LCNet). Keep ProjectionHead class (used for the main projection), ArcMarginProduct (unchanged).

Wait -- actually ProjectionHead is used by FrozenBackboneWithHead. The new LCNet has its own projection (embed_fc + embed_bn). So ProjectionHead class is no longer needed. But keep it in case agent wants to experiment with it. Actually no -- the research says the head structure is fixed: Linear(1280, 256) + BN. The 512-dim hidden layer ProjectionHead is from the old architecture. Remove it.

Actually, looking at the current code: `ProjectionHead` has a hidden_dim=512 two-layer MLP. The new LCNet per D-08 uses `FC(1280) -> BN -> embedding(256) -> BN`. That's a single linear layer with BN. So ProjectionHead is NOT used by the new LCNet.

Remove `ProjectionHead` and `FrozenBackboneWithHead` classes. Replace with `make_divisible`, `SqueezeExcite`, `DepthwiseSeparableConv`, `LCNet`, `load_pretrained_lcnet`.

**F. Update the run_train_epoch function signature** to accept `LCNet` instead of `FrozenBackboneWithHead` in type hints. The actual call `model.forward_embeddings_train(images)` stays the same since LCNet has this method.

**G. Update SWA and checkpoint logic** -- currently references `model.backbone` and `model.proj`. With the new LCNet structure, model.state_dict() and load_state_dict() work the same way, so SWA logic is unchanged. Checkpoint saving is unchanged.

**H. Important: The `build_train_transform` function** uses `timm.create_model(model_name)` to get data config for normalization. Since the custom LCNet doesn't use timm at runtime, change this to hardcode ImageNet normalization (0.485, 0.456, 0.406) / (0.229, 0.224, 0.225) which is what lcnet_050 uses. Or keep the timm lookup for pretrained variants. Simplest: hardcode the ImageNet values since all lcnet variants use them. Update `build_train_transform` to accept mean/std directly or hardcode them, removing the timm model creation.

Actually, `build_train_transform` creates a whole timm model just to get mean/std. This is wasteful. Replace with:
```python
def build_train_transform(image_size: int) -> transforms.Compose:
    mean = (0.485, 0.456, 0.406)  # ImageNet
    std = (0.229, 0.224, 0.225)
    ...
```
Remove the `model_name` parameter. Update the call in `main()`: `train_transform = build_train_transform(IMAGE_SIZE)`.

Similarly, in `main()`, remove the MODEL_NAME usage for model creation (no longer needed). MODEL_NAME constant can be removed or kept as documentation.

Remove `import timm` from the main imports -- timm is only needed inside `load_pretrained_lcnet` and `build_train_transform` (if we keep the lookup). Since we're hardcoding mean/std, timm is only needed in `load_pretrained_lcnet`. Move the `import timm` inside that function.

**Summary of changes to train.py:**
1. Add 6 new constants (LCNET_SCALE, SE_START_BLOCK, SE_REDUCTION, ACTIVATION, KERNEL_SIZES, USE_PRETRAINED)
2. Remove MODEL_NAME constant (or keep for reference)
3. Remove `ProjectionHead` class
4. Remove `FrozenBackboneWithHead` class
5. Add `make_divisible` function
6. Add `SqueezeExcite` class
7. Add `DepthwiseSeparableConv` class
8. Add `LCNet` class with encode(), forward_embeddings_train(), forward_features(), encode_with_spatial(), unfreeze_last_stage()
9. Add `load_pretrained_lcnet` function
10. Update `build_train_transform` to remove timm dependency (hardcode ImageNet normalization)
11. Update `vat_embedding_loss` to use model.forward_features() and model.proj
12. Update `run_train_epoch` type hints
13. Update `main()`: model creation, optimizer param groups, backbone unfreeze logic
14. Move `import timm` from top-level to inside load_pretrained_lcnet
15. Add `"einops>=0.8.0"` to pyproject.toml dependencies
  </action>
  <verify>
    <automated>cd /home/whiskey/workspace/project/central/v2/training/autoresearch && python -c "
import train
import torch

# Constants exist
assert hasattr(train, 'LCNET_SCALE'), 'Missing LCNET_SCALE'
assert hasattr(train, 'SE_START_BLOCK'), 'Missing SE_START_BLOCK'
assert hasattr(train, 'SE_REDUCTION'), 'Missing SE_REDUCTION'
assert hasattr(train, 'ACTIVATION'), 'Missing ACTIVATION'
assert hasattr(train, 'KERNEL_SIZES'), 'Missing KERNEL_SIZES'
assert hasattr(train, 'USE_PRETRAINED'), 'Missing USE_PRETRAINED'
assert train.LCNET_SCALE == 0.5
assert train.USE_PRETRAINED == True

# LCNet class exists with all APIs
assert hasattr(train, 'LCNet'), 'Missing LCNet class'
model = train.LCNet(scale=0.5, embedding_dim=256, device='cpu')
dummy = torch.randn(2, 3, 224, 224)

# encode contract
out = model.encode(dummy)
assert out.shape == (2, 256), f'encode shape: {out.shape}'
norms = torch.norm(out, dim=1)
assert torch.allclose(norms, torch.ones(2), atol=1e-5), 'Not L2-normalized'

# forward_features
spatial, summary = model.forward_features(dummy)
assert len(spatial.shape) == 4, f'spatial should be 4D: {spatial.shape}'
assert spatial.shape[0] == 2
assert spatial.shape[2] == 7 and spatial.shape[3] == 7, f'spatial resolution: {spatial.shape}'
assert summary.shape == (2, 1280), f'summary shape: {summary.shape}'

# encode_with_spatial
emb, sp = model.encode_with_spatial(dummy)
assert emb.shape == (2, 256)
assert sp.shape == spatial.shape

# forward_embeddings_train
train_emb = model.forward_embeddings_train(dummy)
assert train_emb.shape == (2, 256)

# proj attribute for VAT compat
assert hasattr(model, 'proj'), 'Missing proj attribute'

# einops in pyproject.toml
with open('pyproject.toml') as f:
    assert 'einops' in f.read(), 'einops not in pyproject.toml'

# FrozenBackboneWithHead removed
assert not hasattr(train, 'FrozenBackboneWithHead'), 'FrozenBackboneWithHead should be removed'

print('All checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class LCNet" train.py` returns 1
    - `grep -c "class DepthwiseSeparableConv" train.py` returns 1
    - `grep -c "class SqueezeExcite" train.py` returns 1
    - `grep -c "def forward_features" train.py` returns at least 1
    - `grep -c "def encode_with_spatial" train.py` returns at least 1
    - `grep -c "def encode" train.py` returns at least 1
    - `grep -c "LCNET_SCALE" train.py` returns at least 2 (definition + usage)
    - `grep -c "SE_START_BLOCK" train.py` returns at least 2
    - `grep -c "USE_PRETRAINED" train.py` returns at least 2
    - `grep -c "KERNEL_SIZES" train.py` returns at least 2
    - `grep -c "einops" pyproject.toml` returns at least 1
    - `grep -c "FrozenBackboneWithHead" train.py` returns 0
    - `grep -c "def load_pretrained_lcnet" train.py` returns 1
    - `grep -c "make_divisible" train.py` returns at least 2
    - Python verification script above passes
  </acceptance_criteria>
  <done>Custom LCNet class in train.py with DepthwiseSeparableConv, SqueezeExcite, all 6 tunable constants at module level, pretrained weight loading for scale=0.5/0.75/1.0, forward_features/encode/encode_with_spatial/forward_embeddings_train APIs, main() uses LCNet instead of FrozenBackboneWithHead, VAT loss updated for new model API, einops in pyproject.toml, FrozenBackboneWithHead and ProjectionHead removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update test suite for custom LCNet and new constants</name>
  <files>tests/test_train.py</files>
  <read_first>
    - tests/test_train.py (current tests to update)
    - train.py (the updated file from Task 1 -- read to get exact class names, constant values)
  </read_first>
  <action>
Update tests/test_train.py to cover the new LCNet backbone requirements. Changes:

**A. Fix existing broken tests:**

1. `test_constant_values`: Change `assert train.LR == 1e-1` to `assert train.LR == 2e-3` (was already wrong). Remove `assert train.MODEL_NAME == ...` if MODEL_NAME was removed.

2. `test_encode_contract_shape`: Replace `train.FrozenBackboneWithHead(...)` with `train.LCNet(scale=0.5, embedding_dim=256, device='cpu')`.

3. `test_encode_contract_l2_normalized`: Same replacement.

4. `test_encode_contract_no_grad`: Same replacement.

5. `test_training_classes_in_train`: Remove assertions for `FrozenBackboneWithHead` and `ProjectionHead`. Add assertions for `LCNet`, `DepthwiseSeparableConv`, `SqueezeExcite`, `make_divisible`, `load_pretrained_lcnet`.

6. `test_module_level_constants_exist`: Add new constants: `LCNET_SCALE`, `SE_START_BLOCK`, `SE_REDUCTION`, `ACTIVATION`, `KERNEL_SIZES`, `USE_PRETRAINED`. Remove `MODEL_NAME` if it was removed from train.py.

**B. Add new tests for LCNET requirements:**

7. `test_lcnet_tunable_constants` (LCNET-02): Assert all 6 constants exist and have correct default values:
   - LCNET_SCALE == 0.5
   - SE_START_BLOCK == 10
   - SE_REDUCTION == 0.25
   - ACTIVATION == "h_swish"
   - KERNEL_SIZES == [3,3,3,3,3,3,5,5,5,5,5,5,5] (13 elements)
   - USE_PRETRAINED == True

8. `test_forward_features_spatial` (LCNET-04): Create LCNet(scale=0.5), pass dummy [2, 3, 224, 224]. Assert forward_features returns tuple of 2 tensors. Assert spatial shape is [2, C, 7, 7] where C = make_divisible(512 * 0.5) = 256. Assert summary shape is [2, 1280].

9. `test_encode_with_spatial` (LCNET-04): Create LCNet(scale=0.5), pass dummy [2, 3, 224, 224]. Assert encode_with_spatial returns (embedding, spatial). Assert embedding shape [2, 256], L2-normalized. Assert spatial shape [2, C, 7, 7].

10. `test_pretrained_loading` (LCNET-03): Create LCNet(scale=0.5). Call load_pretrained_lcnet(model, 0.5). Assert at least 50% of state_dict keys have non-zero values (i.e., pretrained weights loaded). This test requires network access; if that's a problem, check that the function exists and is callable.

    Simpler alternative: Create LCNet(scale=0.5), save initial state_dict. Call load_pretrained_lcnet. Compare state_dicts -- at least some weights should differ (conv_stem.weight should change from random init to pretrained).

11. `test_lcnet_different_scales` (LCNET-02): Create LCNet(scale=1.0) and LCNet(scale=0.5). Assert different parameter counts (scale=1.0 has more params). Both .encode() should return [B, 256].

12. `test_einops_in_pyproject` (INFRA-09): Read pyproject.toml, assert "einops" appears in the dependencies section.

13. `test_lcnet_no_timm_runtime_dep`: Assert that importing train does NOT create a timm model. Check that `train.LCNet.__init__` source does not contain "timm.create_model". timm should only be used inside load_pretrained_lcnet.

14. `test_encode_contract_shape` (LCNET-01): Already updated above -- uses LCNet, asserts [B, 256] output.
  </action>
  <verify>
    <automated>cd /home/whiskey/workspace/project/central/v2/training/autoresearch && python -m pytest tests/test_train.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_train.py -x -q` passes with 0 failures
    - `grep -c "test_forward_features_spatial" tests/test_train.py` returns 1
    - `grep -c "test_encode_with_spatial" tests/test_train.py` returns 1
    - `grep -c "test_lcnet_tunable_constants" tests/test_train.py` returns 1
    - `grep -c "test_pretrained_loading" tests/test_train.py` returns 1
    - `grep -c "test_einops_in_pyproject" tests/test_train.py` returns 1
    - `grep -c "FrozenBackboneWithHead" tests/test_train.py` returns 0
    - `grep -c "LCNet" tests/test_train.py` returns at least 5
  </acceptance_criteria>
  <done>All tests pass. New tests cover LCNET-01 (.encode contract with LCNet), LCNET-02 (tunable constants + different scales), LCNET-03 (pretrained loading), LCNET-04 (forward_features spatial + encode_with_spatial), INFRA-09 (einops in pyproject.toml). Old FrozenBackboneWithHead references removed. Broken LR assertion fixed.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_train.py -x -q` -- all tests pass
2. `python -c "import train; m = train.LCNet(scale=0.5, device='cpu'); print(m.encode(torch.randn(1,3,224,224)).shape)"` -- outputs torch.Size([1, 256])
3. `grep "einops" pyproject.toml` -- shows einops dependency
4. `grep "class FrozenBackboneWithHead" train.py` -- returns nothing (removed)
5. `grep "class LCNet" train.py` -- returns 1 match
</verification>

<success_criteria>
- Custom LCNet backbone in train.py with .encode() returning L2-normalized Tensor[B, 256]
- forward_features() returns (spatial [B,C,7,7], summary [B,1280]) tuple
- encode_with_spatial() returns (embedding [B,256], spatial [B,C,7,7]) tuple
- 6 tunable constants at module level with correct defaults
- Pretrained weight loading for scale=0.5 via timm
- einops in pyproject.toml
- All tests pass
- FrozenBackboneWithHead removed, no regression in existing test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/05-ssl-custom-lcnet/05-01-SUMMARY.md`
</output>
